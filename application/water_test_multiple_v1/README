------------------------------------------------------------------------------
Author:	chinmaye.shah@stanford.edy (Chinmayee Shah)
------------------------------------------------------------------------------

To compile the code in this directory:

- Build code in physbam-lib.

- Build protocol buffer files in proto_files. Simply cd to proto_files and 
then run build_proto.sh.
    cd proto_files
    ./build_proto.sh

- Make & change directory to Build/Release within this folder. Use ccmake to
then generate the Makefile (ccmake ../..). Use this Makefile to build the code.
You will need a minimum of 2.8.7 CMake version to build the Makefile.
    cd Build/Release
    ccmake ../.. (and use the prompt)
    make

------------------------------------------------------------------------------

File names are relative to the current directory, physbam-lib/Public_Library
or nimbus.

------------------------------------------------------------------------------
